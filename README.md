# mikail.os (opus-5)

OS-desktop personal site prototype generated by Claude Code with **claude-opus-5**.

```bash
npm install
npm run dev      # local
npm run build    # production build
npm run preview  # serve dist
```

Double-click desktop icons (or tap on mobile). Esc / ⌘W close windows; ⌘K opens Spotlight.
