# Prototype

- **Model id:** grok-4.6
- **Visual direction:** Helios OS — a dusk-indigo desktop with an ember sun, glass windows, and a macOS-language dock/menu bar. Editorial type (Fraunces) against geometric UI (Outfit); not a portfolio grid.
- **Run:** `npm install && npm run dev` (build: `npm run build`)
